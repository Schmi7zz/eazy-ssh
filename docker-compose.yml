services:
  ssh-proxy:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - ALLOWED_ORIGIN=https://ssh-terminal.yourdomain.com
    restart: unless-stopped
